#include <stdio.h>
#include <math.h>

int main(){
	int vetA[5], vetB[5], i;
	
	for(i= 0; i <5; i++){      /* inicialização do vetor */ /* percorrendo do vetor */
	 printf("Digite um valor: \n");
	 scanf("%d", &vetA[i]);
}
     for(i= 0; i <5; i++){      /* inicialização do vetor */ /* percorrendo do vetor */
	 vetB[i] = pow(vetA[i],2);
}
	 for(i= 0; i <5; i++){      /* inicialização do vetor */ /* percorrendo do vetor */
	 printf("\n A[%d] = %d  B[%d] = %d", i, vetA[i], i, vetB[i]);
}
	
}
N
